
set hostname if you want since installer won't prompt for it.

find IP address device boots with

setup/configure fw_printenv to get MAC
echo "/dev/mtdblock1 0x00000 0x10000 0x10000" >> /etc/fw_env.config
#can we be sure it will always be mtdblock1?

modify /etc/network/interfaces to hardcode MAC

setup/configure fan control

setup/configure triggerhappy/buttons?

edit fstab for md arrays rather than uuid? add array from before install if desired.

samba config?

notes on how to use LEDs?

other typical installs ntp?
